/**
 * Символическое ядро истории — общее для генератора (client) и движка (game).
 *
 * Зачем один пакет: раньше guard/effect-семантика исполнялась ДВАЖДЫ. Честно —
 * в симуляции генератора; для игры компилятор ТРАНСЛИРОВАЛ её в численные
 * условия рёбер, и движок исполнял уже перевод. Два исполнителя одних правил
 * неизбежно расходились (задокументированный parity gap ±1 фаза). Здесь
 * исполнитель один, поэтому расхождение невозможно по построению.
 *
 * Железное правило нейросимволической схемы: LLM пишет прозу на этапе
 * генерации и НЕ мутирует состояние — любое изменение мира проходит через
 * объявленные effects.
 */

export * from './state';
export * from './brackets';
export * from './guards';
export * from './effects';
export * from './slice';
export * from './calendar';
export * from './rng';
export * from './bundle';
export * from './selector';
export * from './director';
