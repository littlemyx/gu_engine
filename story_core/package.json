{
  "name": "gu-engine-story-core",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "description": "Символическое ядро истории: guard/effect-алгебра, состояние, календарная арифметика, селектор и режиссёр. Общее для генератора (client) и движка (game) — одна семантика, один исполнитель.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}
